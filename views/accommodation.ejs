<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CampusMitra - Accommodations</title>
    <!-- Bootstrap CSS (or your own custom CSS) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .accommodation-card {
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
            overflow: hidden;
        }

        .accommodation-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .accommodation-info {
            padding: 15px;
        }

        .amenities-list li {
            list-style-type: none;
            display: inline-block;
            margin-right: 10px;
            background-color: #f0f0f0;
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 14px;
        }

        .reviews {
            margin-top: 15px;
            font-size: 14px;
        }

        .review {
            margin-bottom: 10px;
            padding: 10px;
            background-color: #f9f9f9;
            border-left: 4px solid #6c757d;
        }
    </style>
</head>

<body>

    <div class="container mt-5">
        <h1 class="text-center mb-4">Accommodations</h1>
        <div class="row">
            <% for( accommodation of accommodations ) { %>
                <div class="col-md-4">
                    <div class="card accommodation-card">
                        <img src="<%= accommodation.images[0] %>" alt="<%= accommodation.name %>">
                        <div class="accommodation-info">
                            <h4 class="card-title">
                                <%= accommodation.name %>
                            </h4>
                            <p><strong>Type:</strong>
                                <%= accommodation.type %>
                            </p>
                            <p><strong>Price:</strong> ₹<%= accommodation.price %>/month</p>
                            <p><strong>Location:</strong>
                                <%= accommodation.location.address %>, <%= accommodation.location.city %>, <%=
                                            accommodation.location.state %> - <%= accommodation.location.pinCode %>
                            </p>
                            <p><strong>Amenities:</strong></p>
                            <ul class="amenities-list">
                                <% for( amenity of accommodation.amenities ) { %>
                                    <li>
                                        <%= amenity %>
                                    </li>
                                    <% } %>

                            </ul>
                            <p><strong>Contact:</strong>
                                <%= accommodation.contactInfo.phone %>
                            </p>
                            <a href="tel:<%= accommodation.contactInfo.phone %>" class="btn btn-primary mt-2">Contact
                                Now</a>
                        </div>

                        <!-- Reviews Section -->
                        <div class="reviews p-3">
                            <h5>Reviews:</h5>
                            <% accommodation.reviews.forEach(function(review) { %>
                                <div class="review">
                                    <strong>
                                        <%= review.reviewerName %>:
                                    </strong>
                                    <span class="text-warning">
                                        <% for(let i=0; i < review.rating; i++) { %>
                                            ★
                                            <% } %>
                                    </span>
                                    <p>
                                        <%= review.comment %>
                                    </p>
                                </div>
                                <% }); %>
                        </div>
                    </div>
                </div>
                <% } %>

        </div>

        <!-- Bootstrap JS (for responsive behavior) -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>