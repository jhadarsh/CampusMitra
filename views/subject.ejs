<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Semester Subjects</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="/styles.css"> <!-- Custom styles -->
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 20px;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        .subject-card {
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            margin: 15px;
            width: 350px;
            padding: 20px;
            transition: transform 0.3s;
        }

        .subject-card:hover {
            transform: scale(1.05);
        }

        .subject-header {
            font-size: 22px;
            font-weight: bold;
            color: #333;
        }

        .subject-code {
            font-size: 14px;
            color: #777;
        }

        .syllabus-list,
        .marks-list {
            list-style: none;
            padding: 0;
        }

        .syllabus-list li,
        .marks-list li {
            padding: 5px 0;
            border-bottom: 1px solid #eee;
        }

        .marks-list li:last-child {
            border-bottom: none;
        }

        .badge {
            background-color: #ff6f61;
            color: white;
            padding: 3px 7px;
            border-radius: 5px;
            font-size: 12px;
        }

        .difficulty {
            text-align: right;
            font-size: 14px;
            color: #555;
        }

        .credit-hours {
            font-size: 14px;
            color: #555;
            margin-top: 10px;
        }
    </style>
</head>

<body>

    <!-- <h1 style="text-align: center; margin-bottom: 30px;">Subjects for Semester < %></h1> -->
 <h1>hi</h1>
    <div class="container">
        <% for( subject of subjects) { %>

            <div class="subject-card">
                <div class="subject-header">
                    <%= subject.name %>
                        <span class="badge">
                            <%= subject.toughnessLevel %>
                        </span>
                </div>
                <div class="subject-code">
                    <i class="fas fa-code"></i>
                    <%= subject.code %>
                </div>

                <h4>Syllabus:</h4>
                <ul class="syllabus-list">
                    <% for( syllabus of subject.syllabus ) { %>
                        <li>
                            <h4>
                                <%= syllabus.unit %>
                            </h4>
                            <p>
                                <%= syllabus.topics.join(", ") %>
                            </p>
                        </li>
                    <% } %>
                 
                </ul>

            <h4>Marks Distribution:</h4>
            <ul class=" marks-list">
                                    <% for( exam of subject.marksDistribution ) { %>
                        <li>
                            <%= exam.examType %> : <%= exam.marks %> marks
                        </li>
                        <% } %>

                </ul>

                <div class="credit-hours">
                    Credit Hours: <strong>
                        <%= subject.creditHours %>
                    </strong>
                </div>

                <div class="difficulty">
                    Toughness Level: <%= subject.toughnessLevel %>
                </div>
            </div>

            <% } %>
    </div>

</body>

</html>